# CLAUDE.md

Always use `uv` with the virtual environment `.venv` when running python code when working in this repo. If there are extra dependencies needed, add them to the `pyproject.toml` and install them in the virtual environment.
